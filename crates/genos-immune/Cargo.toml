[package]
name = "genos-immune"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
uuid = { version = "1.10", features = ["v4", "serde"] }
genos-genome = { path = "../genos-genome" }

