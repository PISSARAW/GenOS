# genos-model

`genos-model` acts as the translation layer between GenOS and various Large Language Models (LLMs).

## Responsibilities
- Structuring LLM prompts and handling structured JSON outputs.
- Dealing with multiple model providers (Anthropic, OpenAI, local models).
- Simulating models for deterministic tests.
