# genos-cli

`genos-cli` is the command-line interface for the GenOS engine.

## Responsibilities
- Entry point for user interactions.
- Commands for creating agents, taking snapshots, running experiments, and managing the development loop.
