{
  "name": "genos_anthony_epigenetic_pointer",
  "description": "Replaces large machine-consumed data (like raw JSON or CLI output) with a lightweight file pointer to preserve the context window without losing exact details.",
  "parameters": {
    "type": "object",
    "properties": {
      "raw_data": {
        "type": "string",
        "description": "The massive raw string or JSON data to store behind a pointer."
      }
    },
    "required": ["raw_data"]
  }
}
