{
  "name": "genos_read_blackboard",
  "description": "STATE Layer. Reads the asynchronous event ledger (the Blackboard). Use this to check the compressed status of tasks launched by intent commands (Explore, Verify, Merge) without having to parse low-level logs.",
  "parameters": {
    "type": "object",
    "properties": {
      "filter_intent_id": {
        "type": "string",
        "description": "Optional. Filters the state for a specific asynchronous intent."
      }
    },
    "required": []
  }
}
