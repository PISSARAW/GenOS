{
  "name": "genos_telemetry_stream",
  "description": "OBSERVER Layer (Rule 7). Subscribes to the live telemetry stream of a hidden executing Swarm. Allows the agent or human to see exactly which biological tools (mitosis, apoptosis) the asynchronous Surgeon is running in the background, providing full explicability.",
  "parameters": {
    "type": "object",
    "properties": {
      "intent_id": {
        "type": "string",
        "description": "The ID of the asynchronous intent/task to monitor."
      }
    },
    "required": ["intent_id"]
  }
}
