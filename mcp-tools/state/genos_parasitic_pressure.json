{
  "name": "genos_parasitic_pressure",
  "description": "Evaluate parasitic pressure on agents based on the given manifest.",
  "parameters": {
    "type": "object",
    "properties": {
      "manifest": {
        "type": "string",
        "description": "Path to the ecosystem manifest containing agents and parasites."
      }
    },
    "required": ["manifest"]
  }
}
