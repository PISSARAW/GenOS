{
  "name": "genos_resilience_circuit_breaker",
  "description": "Calculate LKGS (Last Known Good State) and handle rollbacks with cherry-pick.",
  "parameters": {
    "type": "object",
    "properties": {
      "target_agent": {
        "type": "string",
        "description": "Agent to apply the circuit breaker to"
      },
      "calculate_lkgs": {
        "type": "boolean",
        "description": "Whether to calculate the Last Known Good State"
      },
      "rollback": {
        "type": "boolean",
        "description": "Whether to perform a rollback"
      },
      "cherry_pick": {
        "type": "boolean",
        "description": "Whether to cherry-pick safe operations during rollback"
      }
    },
    "required": ["target_agent", "calculate_lkgs"]
  }
}
