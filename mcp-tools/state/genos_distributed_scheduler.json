{
  "name": "genos_distributed_scheduler",
  "description": "Start the cooperative distributed scheduler.",
  "parameters": {
    "type": "object",
    "properties": {
      "workers": { "type": "number", "description": "Number of workers" }
    },
    "required": ["workers"]
  }
}
