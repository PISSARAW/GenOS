{
  "name": "genos_export_audit",
  "description": "Export an audit bundle for a specific snapshot.",
  "parameters": {
    "type": "object",
    "properties": {
      "snapshot_id": {
        "type": "string",
        "description": "The snapshot ID to audit"
      },
      "output": {
        "type": "string",
        "description": "The output file path"
      }
    },
    "required": ["snapshot_id", "output"]
  }
}
