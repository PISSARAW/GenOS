{
  "name": "genos_anthony_methylation",
  "description": "Timestamps and seals a Ground Truth variable to prevent tautological tests (comparing two dynamically generated buggy outputs).",
  "parameters": {
    "type": "object",
    "properties": {
      "ground_truth_data": {
        "type": "string",
        "description": "The exact expected data to seal."
      }
    },
    "required": ["ground_truth_data"]
  }
}
