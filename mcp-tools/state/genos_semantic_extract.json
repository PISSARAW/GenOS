{
  "name": "genos_semantic_extract",
  "description": "Extract structured semantic claims from arbitrary prose according to ADR 0016.",
  "parameters": {
    "type": "object",
    "properties": {
      "prose": {
        "type": "string",
        "description": "The prose text from which to extract claims"
      },
      "threshold": {
        "type": "number",
        "description": "Confidence threshold to filter extracted claims"
      }
    },
    "required": ["prose", "threshold"]
  }
}
