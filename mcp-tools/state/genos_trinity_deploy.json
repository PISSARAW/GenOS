{
  "name": "genos_trinity_deploy",
  "description": "Deploy three isolated GenOS agents to solve a mission using distinct strategies.",
  "parameters": {
    "type": "object",
    "properties": {
      "mission_id": { "type": "string" },
      "strategies": { "type": "string", "description": "Comma-separated list of 3 strategy profiles" }
    },
    "required": ["mission_id", "strategies"]
  }
}
