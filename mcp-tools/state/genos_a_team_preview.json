{
  "name": "genos_a_team_preview",
  "description": "Analyzes a complex project requirement and dynamically provisions an elite GenOS A-Team (Swarm). This tool formally defines the specialized roles, partitions the tasks according to GenOS constraints (max 400 lines/file, max 3 params/function), and automatically incorporates the mandatory 'telemetry_observer' agent before orchestration.",
  "parameters": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "additionalProperties": false,
    "properties": {
      "project_goal": {
        "description": "Detailed description of the final objective for the A-Team.",
        "type": "string",
        "minLength": 10
      },
      "sub_systems": {
        "description": "List of decoupled subsystems the project is divided into (e.g., 'Backend', 'Frontend', 'Database', 'QA').",
        "type": "array",
        "items": { "type": "string" }
      },
      "assigned_roles": {
        "description": "List of tailored roles that will be created and orchestrated for this swarm. Must include 'telemetry_observer'.",
        "type": "array",
        "items": { "type": "string" }
      },
      "enforce_genos_rules": {
        "description": "Whether to enforce strict GenOS rules (max 400 lines, max 3 parameters, visual style) across all agents in the swarm. Should always be true.",
        "type": "boolean"
      }
    },
    "required": ["project_goal", "sub_systems", "assigned_roles", "enforce_genos_rules"],
    "type": "object"
  }
}
