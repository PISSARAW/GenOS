{
  "name": "genos_anthony_p53_checkpoint",
  "description": "Enforces GenOS governance policies directly as an allosteric inhibitor (p53 Checkpoint). Blocks operations that violate RBAC security, file size limits, or strict aesthetic rules, regardless of agent prompt intentions.",
  "parameters": {
    "type": "object",
    "properties": {
      "action_type": {
        "type": "string",
        "description": "The type of action attempted (e.g., WRITE_FILE, UPDATE_FRONTEND)."
      },
      "clearance_level": {
        "type": "string",
        "description": "The RBAC clearance level of the executing agent (e.g., DEFAULT, ADMIN)."
      },
      "payload": {
        "type": "string",
        "description": "The content or path related to the action to be validated."
      }
    },
    "required": ["action_type", "clearance_level", "payload"]
  }
}
