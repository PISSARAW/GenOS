{
  "name": "genos_incident_experiment",
  "description": "Run an Adaptive Incident Search to reproduce production crashes by forking and mutating universes deterministically.",
  "parameters": {
    "type": "object",
    "properties": {
      "manifest": {
        "type": "string",
        "description": "Path to the incident manifest (YAML/JSON) defining the evidence and mutation dimensions"
      }
    },
    "required": ["manifest"]
  }
}
