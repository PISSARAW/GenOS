{
  "name": "genos_bisect_agent",
  "description": "Bisect an agent trajectory to locate the first bad event or memory.",
  "parameters": {
    "type": "object",
    "properties": {
      "agent_id": {
        "type": "string",
        "description": "The ID of the agent to bisect."
      },
      "predicate": {
        "type": "string",
        "description": "The failure predicate to test."
      }
    },
    "required": ["agent_id", "predicate"]
  }
}
