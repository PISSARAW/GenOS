{
  "name": "genos_synaptic_stdp_update",
  "description": "Trigger an O(1) cognitive mutation by updating the STDP synaptic weights of agent prompt traits based on outcome.",
  "parameters": {
    "type": "object",
    "properties": {
      "agent_id": { "type": "string" },
      "trait": { "type": "string" },
      "outcome_score": { "type": "number", "description": "Score between -1.0 and 1.0" }
    },
    "required": ["agent_id", "trait", "outcome_score"]
  }
}
