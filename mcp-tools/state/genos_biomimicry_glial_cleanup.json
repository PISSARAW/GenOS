{
  "name": "genos_biomimicry_glial_cleanup",
  "description": "Active les cellules gliales pour nettoyer le contexte de l'agent, archiver les vieux messages, supprimer les fichiers temporaires et optimiser la mémoire.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent_id": { "type": "string" },
      "intensity": { "type": "string", "enum": ["light", "deep_sleep_purge"] }
    },
    "required": ["agent_id"]
  }
}
