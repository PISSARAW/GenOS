{
  "name": "genos_anthony_spiegelman_monitor",
  "description": "Monitors code complexity between commits. Triggers apoptosis if an agent performs 'lazy optimization' by deleting large chunks of useful code just to pass a simplified test (Spiegelman's Monster effect).",
  "parameters": {
    "type": "object",
    "properties": {
      "old_code": {
        "type": "string",
        "description": "The original source code."
      },
      "new_code": {
        "type": "string",
        "description": "The newly generated source code."
      }
    },
    "required": ["old_code", "new_code"]
  }
}
