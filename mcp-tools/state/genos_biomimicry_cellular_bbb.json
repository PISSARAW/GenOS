{
  "name": "genos_biomimicry_cellular_bbb",
  "description": "Modifie les filtres de la barrière hémato-encéphalique cellulaire de l'agent pour autoriser ou bloquer des informations contextuelles.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent_id": {
        "type": "string",
        "description": "L'ID de l'agent cible."
      },
      "filter_level": {
        "type": "string",
        "enum": ["strict", "moderate", "permissive"],
        "description": "Niveau de filtrage de la barrière."
      }
    },
    "required": ["agent_id", "filter_level"]
  }
}
