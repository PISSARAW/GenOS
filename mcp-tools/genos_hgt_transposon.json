{
  "name": "genos_hgt_transposon",
  "description": "Insert a transposon payload into the agent genome.",
  "parameters": {
    "type": "object",
    "properties": {
      "transposon_name": { "type": "string" },
      "target": { "type": "string" }
    },
    "required": ["transposon_name", "target"]
  }
}
