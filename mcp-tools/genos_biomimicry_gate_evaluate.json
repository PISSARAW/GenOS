{
  "name": "genos_biomimicry_gate_evaluate",
  "description": "Evaluate biological gates to determine whether progression to the next phase is allowed.",
  "parameters": {
    "type": "object",
    "properties": {
      "phase": { "type": "string", "description": "Phase to evaluate (init, fork, run, diff, merge)" },
      "facts": { "type": "array", "items": { "type": "string" }, "description": "List of facts as key=value strings" }
    },
    "required": ["phase", "facts"]
  }
}
