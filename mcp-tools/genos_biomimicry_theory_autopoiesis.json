{
  "name": "genos_biomimicry_theory_autopoiesis",
  "description": "Déclenche l'auto-maintenance de l'agent en lui permettant d'ajuster son propre génome et ses paramètres (autopoïèse).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent_id": {
        "type": "string",
        "description": "L'ID de l'agent effectuant l'autopoïèse."
      },
      "target_gene": {
        "type": "string",
        "description": "Le paramètre du génome à modifier (ex: risk_tolerance, exploration)."
      },
      "new_value": {
        "type": "number",
        "description": "La nouvelle valeur à appliquer."
      }
    },
    "required": ["agent_id", "target_gene", "new_value"]
  }
}
