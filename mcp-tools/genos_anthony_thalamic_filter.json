{
  "name": "genos_anthony_thalamic_filter",
  "description": "Filters disposable context from logs to avoid overloading the agent's context window. Keeps only critical anomalies and deltas.",
  "parameters": {
    "type": "object",
    "properties": {
      "logs": {
        "type": "array",
        "items": { "type": "string" },
        "description": "Array of log strings to filter."
      }
    },
    "required": ["logs"]
  }
}
