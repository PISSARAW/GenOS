{
  "name": "genos_biomimicry_network_quorum",
  "description": "Évalue si la population d'agents locaux est suffisante pour déclencher une action collective (refactoring, attaque, etc).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent_id": { "type": "string" },
      "quorum_threshold": { "type": "number", "description": "Nombre minimum d'agents requis pour l'action" },
      "action_id": { "type": "string" }
    },
    "required": ["agent_id", "quorum_threshold", "action_id"]
  }
}
