{
  "name": "genos_verify",
  "description": "INTENT Layer. Asynchronous command (Fire and Forget). Requests the verification of a branch. A 'Surgeon' sub-agent will run the tests and use the thymus saboteur in the background. The compressed state (Success or False Positive) will be published on the Blackboard.",
  "parameters": {
    "type": "object",
    "properties": {
      "branch_id": {
        "type": "string",
        "description": "The ID of the branch or capsule to verify."
      }
    },
    "required": ["branch_id"]
  }
}
