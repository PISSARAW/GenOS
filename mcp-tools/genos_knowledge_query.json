{
  "name": "genos_knowledge_query",
  "description": "KNOWLEDGE Layer. Dynamically queries the biomimetic documentation of GenOS to understand a concept or find the right low-level tool without saturating the prompt context.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The biological concept or business problem (e.g., 'apoptosis', 'memory leak', 'resilience')."
      }
    },
    "required": ["query"]
  }
}
