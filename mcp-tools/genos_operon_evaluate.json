{
  "name": "genos_operon_evaluate",
  "description": "Evaluate an operon for expression.",
  "parameters": {
    "type": "object",
    "properties": {
      "operon_id": { "type": "string" }
    },
    "required": ["operon_id"]
  }
}
