{
  "name": "genos_merge",
  "description": "INTENT Layer. Asynchronous command (Fire and Forget). The agent chooses the winning branch. A sub-agent takes care of killing the other branches (apoptosis) and merging the code. Result updated on the Blackboard.",
  "parameters": {
    "type": "object",
    "properties": {
      "winning_branch_id": {
        "type": "string",
        "description": "The ID of the branch that was verified as successful and should be merged."
      }
    },
    "required": ["winning_branch_id"]
  }
}
