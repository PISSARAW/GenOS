{
  "name": "genos_causal_replay_experiment",
  "description": "Analyze causal boundaries in GenOS and trace logical effects across branches.",
  "parameters": {
    "type": "object",
    "properties": {
      "input_file": {
        "type": "string",
        "description": "Path to the JSON file containing causal boundaries"
      },
      "output_file": {
        "type": "string",
        "description": "Path where the causal analysis report will be written"
      }
    },
    "required": ["input_file", "output_file"]
  }
}
