{
  "name": "genos_cost_accounting",
  "description": "Calculate and report cost accounting for agents and resources.",
  "parameters": {
    "type": "object",
    "properties": {
      "agent_id": {
        "type": "string",
        "description": "Agent ID to compute costs for"
      },
      "timeframe": {
        "type": "string",
        "description": "Timeframe for accounting (e.g. '7d', '24h')"
      }
    },
    "required": ["agent_id"]
  }
}
