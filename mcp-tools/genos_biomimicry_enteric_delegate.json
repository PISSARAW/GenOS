{
  "name": "genos_biomimicry_enteric_delegate",
  "description": "Délègue l'ingestion et l'analyse de données massives (logs, gros fichiers) au système nerveux entérique, qui agit en arrière-plan et ne remonte que des rapports d'état (nerf vague).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent_id": { "type": "string" },
      "data_source": { "type": "string" },
      "digestion_mode": { "type": "string", "enum": ["slow", "fast", "extract_errors"] }
    },
    "required": ["agent_id", "data_source"]
  }
}
