{
  "name": "genos_resilience_apoptosis",
  "description": "Trigger apoptosis for an agent to gracefully terminate and clean up its state.",
  "parameters": {
    "type": "object",
    "properties": {
      "agent_id": {
        "type": "string",
        "description": "The ID of the agent to terminate."
      }
    },
    "required": ["agent_id"]
  }
}
