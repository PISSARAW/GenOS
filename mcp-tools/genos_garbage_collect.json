{
  "name": "genos_garbage_collect",
  "description": "INTENT Layer. Triggers the cognitive garbage collection of the Blackboard. Semantically compresses old states and removes obsolete async events to prevent context bloating and Blackboard poisoning.",
  "parameters": {
    "type": "object",
    "properties": {
      "strategy": {
        "type": "string",
        "description": "Optional. The compression strategy (e.g., 'aggressive', 'summarize_only')."
      }
    },
    "required": []
  }
}
