{
  "name": "genos_compliance_report",
  "description": "Generate a compliance report for the agent swarm (e.g., EU_AI_ACT, SOC_2).",
  "parameters": {
    "type": "object",
    "properties": {
      "standard": { "type": "string", "enum": ["EU_AI_ACT", "SOC_2", "HIPAA"] },
      "output_file": { "type": "string" }
    },
    "required": ["standard", "output_file"]
  }
}
