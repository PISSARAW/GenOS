{
  "name": "genos_allostatic_planning",
  "description": "Allostatic Planning. Launches predictive simulations of intended actions and evaluates their viability before expending metabolic tokens.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "description": "Action to take: 'predict' or 'evaluate'."
      },
      "plan_action": {
        "type": "string",
        "description": "Action being planned (if action=predict)."
      },
      "expected": {
        "type": "string",
        "description": "Expected outcome of the plan (if action=predict)."
      },
      "cost": {
        "type": "number",
        "description": "Estimated token metabolic cost (if action=predict)."
      },
      "score": {
        "type": "number",
        "description": "Validation score of the evidence (if action=evaluate)."
      }
    },
    "required": ["action"]
  }
}
