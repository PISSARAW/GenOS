{
  "name": "genos_active_sensing",
  "description": "Active Sensing (Echolocation). Emits rapid clarification clicks to map user constraints implicitly before executing an action. Replaces standard grill-me.",
  "parameters": {
    "type": "object",
    "properties": {
      "focus": {
        "type": "string",
        "description": "The specific area of constraint to clarify."
      },
      "ambiguity": {
        "type": "number",
        "description": "The degree of ambiguity (0.0 to 1.0) prompting this click."
      }
    },
    "required": ["focus", "ambiguity"]
  }
}
