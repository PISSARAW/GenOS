# genos-mcp

`genos-mcp` provides a Model Context Protocol (MCP) server for GenOS.

## Responsibilities
- Implementing the MCP spec for LLM integration.
- Exposing GenOS core functionality directly to MCP clients (like AI assistants).
