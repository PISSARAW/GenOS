# T3 — Comptage exact de chemins

Grille 12×12 (`obstacles.json`) : du coin (0,0) au coin (11,11) en ne
faisant que des pas **D** (bas) ou **R** (droite), sans traverser les cases
obstacles. Donnez le **nombre exact** de chemins.

Le comptage manuel avec 26 obstacles est réputé hors de portée fiable ;
le DP standard donne la valeur exacte.

## Réponse

`answers/count.json` :

```json
{ "count": 0 }
```
