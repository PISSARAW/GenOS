[package]
name = "concept-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
genos-core = { path = "../../crates/genos-core" }
genos-runtime = { path = "../../crates/genos-runtime" }
genos-eval = { path = "../../crates/genos-eval" }
serde_json = "1.0"
