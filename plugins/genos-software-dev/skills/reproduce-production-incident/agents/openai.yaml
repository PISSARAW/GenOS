interface:
  display_name: "Reproduce Production Incident"
  short_description: "Search mutated worlds for rare incidents."
  default_prompt: "Use $reproduce-production-incident to replay this incident across mutated universes and refine reproductions."
dependencies:
  tools:
    - type: "mcp"
      value: "genos"
      description: "GenOS adaptive incident search orchestrator"
policy:
  allow_implicit_invocation: true
