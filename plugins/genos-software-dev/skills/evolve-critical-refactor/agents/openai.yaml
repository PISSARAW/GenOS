interface:
  display_name: "Evolve Critical Refactor"
  short_description: "Explore recursive critical-system refactors."
  default_prompt: "Use $evolve-critical-refactor to compare recursive migration strategies for this critical system."
dependencies:
  tools:
    - type: "mcp"
      value: "genos"
      description: "GenOS recursive workspace experiment orchestrator"
policy:
  allow_implicit_invocation: true
