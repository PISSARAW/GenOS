interface:
  display_name: "Replay Causal History"
  short_description: "Re-run history from a past decision point."
  default_prompt: "Use $replay-causal-history to fork this past decision and explain the resulting causal differences."
dependencies:
  tools:
    - type: "mcp"
      value: "genos"
      description: "GenOS temporal causal replay orchestrator"
policy:
  allow_implicit_invocation: true
