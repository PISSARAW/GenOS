//! Generated by splitting schema.js (400-line gate). Do not edit SQL here.
const TABLES_EXTENSIONS_BASE = [
"CREATE TABLE IF NOT EXISTS cryptobiosis_snapshots (",
"    snapshot_id TEXT PRIMARY KEY,",
"    agent_id TEXT NOT NULL,",
"    workspace_id TEXT,",
"    capsule_hash TEXT NOT NULL,",
"    status TEXT NOT NULL CHECK (status IN ('freezing', 'frozen', 'thawing', 'thawed', 'failed')),",
"    metadata_json TEXT NOT NULL DEFAULT '{}',",
"    frozen_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    thawed_at DATETIME,",
"    FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE,",
"    FOREIGN KEY (workspace_id) REFERENCES workspaces(id) ON DELETE SET NULL",
");",
"",
"CREATE TABLE IF NOT EXISTS orchestration_action_receipts (",
"    receipt_key TEXT PRIMARY KEY,",
"    orchestrator_id TEXT NOT NULL,",
"    source_event_id TEXT NOT NULL,",
"    tool TEXT NOT NULL,",
"    status TEXT NOT NULL CHECK (status IN ('started', 'completed', 'failed')),",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    completed_at DATETIME",
");",
"",
"CREATE TABLE IF NOT EXISTS conscience_transitions (",
"    id INTEGER PRIMARY KEY AUTOINCREMENT,",
"    agent_id TEXT NOT NULL,",
"    from_revision INTEGER NOT NULL,",
"    to_revision INTEGER NOT NULL,",
"    from_dissonance REAL NOT NULL,",
"    to_dissonance REAL NOT NULL,",
"    from_budget REAL NOT NULL,",
"    to_budget REAL NOT NULL,",
"    from_apoptotic INTEGER NOT NULL,",
"    to_apoptotic INTEGER NOT NULL,",
"    reason TEXT NOT NULL DEFAULT 'evaluation',",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE",
");",
"",
"-- 22. Runtime execution of an immutable strategy contract",
"CREATE TABLE IF NOT EXISTS strategy_execution_runs (",
"    id TEXT PRIMARY KEY,",
"    agent_id TEXT NOT NULL,",
"    contract_id TEXT NOT NULL,",
"    contract_version INTEGER NOT NULL,",
"    status TEXT NOT NULL DEFAULT 'planned' CHECK (status IN ('planned', 'running', 'awaiting_approval', 'completed', 'failed', 'blocked', 'cancelled')),",
"    budget_json TEXT NOT NULL DEFAULT '{}',",
"    metrics_json TEXT NOT NULL DEFAULT '{}',",
"    guardrail_reason TEXT,",
"    started_at DATETIME,",
"    completed_at DATETIME,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE,",
"    FOREIGN KEY (contract_id) REFERENCES strategy_contracts(id) ON DELETE RESTRICT",
");",
"",
"CREATE TABLE IF NOT EXISTS strategy_execution_steps (",
"    id TEXT PRIMARY KEY,",
"    run_id TEXT NOT NULL,",
"    sequence INTEGER NOT NULL,",
"    stage_key TEXT NOT NULL,",
"    status TEXT NOT NULL DEFAULT 'planned' CHECK (status IN ('planned', 'running', 'awaiting_approval', 'completed', 'failed', 'skipped', 'blocked')),",
"    strategy_ids_json TEXT NOT NULL DEFAULT '[]',",
"    planned_budget_json TEXT NOT NULL DEFAULT '{}',",
"    actual_metrics_json TEXT NOT NULL DEFAULT '{}',",
"    evidence_json TEXT NOT NULL DEFAULT '[]',",
"    started_at DATETIME,",
"    completed_at DATETIME,",
"    UNIQUE(run_id, sequence),",
"    FOREIGN KEY (run_id) REFERENCES strategy_execution_runs(id) ON DELETE CASCADE",
");",
"",
"-- 23. Versioned Studio migrations (safe, idempotent upgrades)",
"CREATE TABLE IF NOT EXISTS schema_migrations (",
"    version TEXT PRIMARY KEY,",
"    description TEXT NOT NULL,",
"    applied_at DATETIME DEFAULT CURRENT_TIMESTAMP",
");",
"",
"-- 24. Compliance report history and export metadata",
"CREATE TABLE IF NOT EXISTS compliance_reports (",
"    id TEXT PRIMARY KEY,",
"    framework TEXT NOT NULL CHECK (framework IN ('EU_AI_ACT', 'SOC_2', 'HIPAA')),",
"    workspace_id TEXT,",
"    organization_id TEXT,",
"    project_id TEXT,",
"    status TEXT NOT NULL DEFAULT 'generated' CHECK (status IN ('generated', 'archived')),",
"    score REAL NOT NULL,",
"    findings_json TEXT NOT NULL DEFAULT '[]',",
"    evidence_json TEXT NOT NULL DEFAULT '[]',",
"    generated_by TEXT NOT NULL DEFAULT 'studio',",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY (workspace_id) REFERENCES workspaces(id) ON DELETE SET NULL",
");",
"",
"-- 25. IDE installations use the same signed command contract",
"CREATE TABLE IF NOT EXISTS ide_integrations (",
"    id TEXT PRIMARY KEY,",
"    ide TEXT NOT NULL CHECK (ide IN ('vscode', 'jetbrains', 'antigravity')),",
"    workspace_id TEXT,",
"    client_id TEXT,",
"    version TEXT NOT NULL,",
"    status TEXT NOT NULL DEFAULT 'connected' CHECK (status IN ('connected', 'revoked')),",
"    last_seen_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    metadata_json TEXT NOT NULL DEFAULT '{}',",
"    FOREIGN KEY (workspace_id) REFERENCES workspaces(id) ON DELETE CASCADE",
");",
"CREATE INDEX IF NOT EXISTS idx_ide_status_seen ON ide_integrations(status, last_seen_at DESC);",
"",
"-- Compatibility / Dashboard stats",
"CREATE TABLE IF NOT EXISTS heatmap_activity (",
"    id INTEGER PRIMARY KEY AUTOINCREMENT,",
"    day INTEGER,",
"    actions INTEGER",
");",
"CREATE TABLE IF NOT EXISTS system_stats (",
"    id INTEGER PRIMARY KEY,",
"    total_actions INTEGER,",
"    total_snapshots INTEGER,",
"    total_tasks INTEGER,",
"    total_swarms INTEGER",
");",
"",
"CREATE TABLE IF NOT EXISTS evaluation_runs (",
"    id TEXT PRIMARY KEY,",
"    benchmark TEXT NOT NULL,",
"    model_version TEXT,",
"    prompt_hash TEXT,",
"    genome_hash TEXT,",
"    config_hash TEXT,",
"    score REAL,",
"    brier_score REAL,",
"    abstained INTEGER DEFAULT 0,",
"    result_json TEXT DEFAULT '{}',",
"    agent_id TEXT,",
"    organization_id TEXT,",
"    project_id TEXT,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP",
");",
"",
"CREATE TABLE IF NOT EXISTS provenance_records (",
"    id TEXT PRIMARY KEY,",
"    subject_type TEXT NOT NULL,",
"    subject_id TEXT NOT NULL,",
"    payload_hash TEXT NOT NULL,",
"    parent_hash TEXT,",
"    algorithm TEXT NOT NULL DEFAULT 'sha256',",
"    payload_json TEXT NOT NULL,",
"    organization_id TEXT,",
"    project_id TEXT,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP",
");",
"",
"CREATE TABLE IF NOT EXISTS notification_preferences (",
"    event_type TEXT NOT NULL,",
"    enabled INTEGER NOT NULL DEFAULT 1,",
"    channels_json TEXT NOT NULL DEFAULT '[\"studio\"]',",
"    threshold REAL,",
"    organization_id TEXT NOT NULL DEFAULT '',",
"    project_id TEXT NOT NULL DEFAULT '',",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    PRIMARY KEY (event_type, organization_id, project_id)",
");",
"",
"-- 26. Versioned visual workflows and their execution requests",
"CREATE TABLE IF NOT EXISTS workflows (",
"    id TEXT PRIMARY KEY,",
"    workspace_id TEXT,",
"    name TEXT NOT NULL,",
"    description TEXT DEFAULT '',",
"    version INTEGER NOT NULL DEFAULT 1,",
"    status TEXT NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'staging', 'published', 'archived')),",
"    graph_json TEXT NOT NULL DEFAULT '{\"nodes\":[],\"edges\":[]}',",
"    metadata_json TEXT NOT NULL DEFAULT '{}',",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY (workspace_id) REFERENCES workspaces(id) ON DELETE SET NULL",
");",
"",
"CREATE TABLE IF NOT EXISTS workflow_versions (",
"    id TEXT PRIMARY KEY,",
"    workflow_id TEXT NOT NULL,",
"    version INTEGER NOT NULL,",
"    graph_json TEXT NOT NULL DEFAULT '{\"nodes\":[],\"edges\":[]}',",
"    metadata_json TEXT NOT NULL DEFAULT '{}',",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY (workflow_id) REFERENCES workflows(id) ON DELETE CASCADE,",
"    UNIQUE(workflow_id, version)",
");",
"",
"CREATE TABLE IF NOT EXISTS workflow_runs (",
"    id TEXT PRIMARY KEY,",
"    workflow_id TEXT NOT NULL,",
"    workflow_version INTEGER NOT NULL,",
"    organization_id TEXT,",
"    project_id TEXT,",
"    priority INTEGER NOT NULL DEFAULT 0,",
"    status TEXT NOT NULL DEFAULT 'queued' CHECK (status IN ('queued', 'running', 'completed', 'failed', 'cancelled')),",
"    input_json TEXT NOT NULL DEFAULT '{}',",
"    output_json TEXT,",
"    error_json TEXT,",
"    attempts INTEGER NOT NULL DEFAULT 0,",
"    max_attempts INTEGER NOT NULL DEFAULT 3,",
"    timeout_ms INTEGER NOT NULL DEFAULT 30000,",
"    claimed_at DATETIME,",
"    next_attempt_at DATETIME,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    started_at DATETIME,",
"    completed_at DATETIME,",
"    FOREIGN KEY (workflow_id) REFERENCES workflows(id) ON DELETE CASCADE",
");",
"",
"-- 27. Prompt registry and immutable prompt versions",
"CREATE TABLE IF NOT EXISTS prompts (",
"    id TEXT PRIMARY KEY,",
"    name TEXT NOT NULL UNIQUE,",
"    current_version INTEGER NOT NULL DEFAULT 1,",
"    variables_json TEXT NOT NULL DEFAULT '[]',",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP",
");",
"CREATE TABLE IF NOT EXISTS prompt_versions (",
"    id TEXT PRIMARY KEY,",
"    prompt_id TEXT NOT NULL,",
"    version INTEGER NOT NULL,",
"    template TEXT NOT NULL,",
"    model TEXT NOT NULL DEFAULT '',",
"    config_json TEXT NOT NULL DEFAULT '{}',",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY (prompt_id) REFERENCES prompts(id) ON DELETE CASCADE,",
"    UNIQUE(prompt_id, version)",
");",
"",
"-- 28. Workspace evaluation datasets and batch jobs",
"CREATE TABLE IF NOT EXISTS evaluation_campaigns (id TEXT PRIMARY KEY, name TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'planned' CHECK (status IN ('planned', 'running', 'completed', 'failed', 'cancelled')), seed TEXT, config_json TEXT NOT NULL DEFAULT '{}', organization_id TEXT NOT NULL, project_id TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
"CREATE TABLE IF NOT EXISTS datasets (id TEXT PRIMARY KEY, name TEXT NOT NULL, description TEXT DEFAULT '', metadata_json TEXT NOT NULL DEFAULT '{}', organization_id TEXT, project_id TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, UNIQUE(organization_id, project_id, name));",
"CREATE TABLE IF NOT EXISTS dataset_cases (id TEXT PRIMARY KEY, dataset_id TEXT NOT NULL, input_json TEXT NOT NULL DEFAULT '{}', expected_json TEXT, labels_json TEXT NOT NULL DEFAULT '[]', created_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(dataset_id) REFERENCES datasets(id) ON DELETE CASCADE);",
"CREATE TABLE IF NOT EXISTS evaluation_jobs (id TEXT PRIMARY KEY, dataset_id TEXT, campaign_id TEXT, priority INTEGER NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'queued', config_json TEXT NOT NULL DEFAULT '{}', result_json TEXT, error_json TEXT, attempts INTEGER NOT NULL DEFAULT 0, max_attempts INTEGER NOT NULL DEFAULT 3, claimed_at DATETIME, next_attempt_at DATETIME, organization_id TEXT, project_id TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, completed_at DATETIME, FOREIGN KEY(dataset_id) REFERENCES datasets(id) ON DELETE SET NULL, FOREIGN KEY(campaign_id) REFERENCES evaluation_campaigns(id) ON DELETE SET NULL);",
"",
"-- 29. RAG document, chunk and retrieval records",
"CREATE TABLE IF NOT EXISTS rag_documents (id TEXT PRIMARY KEY, name TEXT NOT NULL, content_length INTEGER NOT NULL DEFAULT 0, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
"CREATE TABLE IF NOT EXISTS rag_chunks (id TEXT PRIMARY KEY, document_id TEXT NOT NULL, chunk_index INTEGER NOT NULL, content TEXT NOT NULL, embedding_json TEXT, embedding_blob BLOB, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(document_id) REFERENCES rag_documents(id) ON DELETE CASCADE);",
"",
"-- 30. Installed connectors, plugins and their tested schemas",
"CREATE TABLE IF NOT EXISTS integrations (id TEXT PRIMARY KEY, name TEXT NOT NULL UNIQUE, type TEXT NOT NULL DEFAULT 'connector', status TEXT NOT NULL DEFAULT 'installed', config_json TEXT NOT NULL DEFAULT '{}', created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
"",
"-- 31. Controlled workflow releases and rollback state",
"CREATE TABLE IF NOT EXISTS releases (id TEXT PRIMARY KEY, workflow_id TEXT NOT NULL, version INTEGER NOT NULL, environment TEXT NOT NULL DEFAULT 'staging', traffic REAL NOT NULL DEFAULT 100, status TEXT NOT NULL DEFAULT 'pending', created_at DATETIME DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(workflow_id) REFERENCES workflows(id) ON DELETE CASCADE);",
"",
"-- 31b. Progressive delivery, SLOs and attributable project usage",
"CREATE TABLE IF NOT EXISTS release_rollouts (",
"    id TEXT PRIMARY KEY,",
"    release_id TEXT NOT NULL,",
"    organization_id TEXT NOT NULL,",
"    project_id TEXT NOT NULL,",
"    strategy TEXT NOT NULL CHECK (strategy IN ('canary', 'ab')),",
"    status TEXT NOT NULL DEFAULT 'running' CHECK (status IN ('running', 'promoted', 'rolled_back', 'paused')),",
"    config_json TEXT NOT NULL DEFAULT '{}',",
"    decision_json TEXT,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY(release_id) REFERENCES releases(id) ON DELETE CASCADE",
");",
"CREATE TABLE IF NOT EXISTS release_rollout_metrics (",
"    rollout_id TEXT NOT NULL,",
"    variant TEXT NOT NULL,",
"    requests INTEGER NOT NULL DEFAULT 0,",
"    errors INTEGER NOT NULL DEFAULT 0,",
"    latency_ms_total REAL NOT NULL DEFAULT 0,",
"    tokens INTEGER NOT NULL DEFAULT 0,",
"    cost_usd REAL NOT NULL DEFAULT 0,",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    PRIMARY KEY(rollout_id, variant),",
"    FOREIGN KEY(rollout_id) REFERENCES release_rollouts(id) ON DELETE CASCADE",
");",
"CREATE TABLE IF NOT EXISTS release_slo_policies (",
"    id TEXT PRIMARY KEY,",
"    organization_id TEXT NOT NULL,",
"    project_id TEXT NOT NULL,",
"    name TEXT NOT NULL,",
"    max_error_rate REAL NOT NULL DEFAULT 0.01,",
"    max_p95_latency_ms REAL NOT NULL DEFAULT 3000,",
"    min_requests INTEGER NOT NULL DEFAULT 100,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    UNIQUE(organization_id, project_id, name)",
");",
"CREATE TABLE IF NOT EXISTS usage_ledger (",
"    id TEXT PRIMARY KEY,",
"    organization_id TEXT NOT NULL,",
"    project_id TEXT NOT NULL,",
"    release_id TEXT,",
"    category TEXT NOT NULL,",
"    quantity REAL NOT NULL DEFAULT 0,",
"    cost_usd REAL NOT NULL DEFAULT 0,",
"    metadata_json TEXT NOT NULL DEFAULT '{}',",
"    occurred_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY(release_id) REFERENCES releases(id) ON DELETE SET NULL",
");",
"",
"-- 31c. Immutable artifact registries and shareable marketplace listings",
"CREATE TABLE IF NOT EXISTS registry_artifacts (",
"    id TEXT PRIMARY KEY,",
"    organization_id TEXT NOT NULL,",
"    project_id TEXT NOT NULL,",
"    kind TEXT NOT NULL CHECK (kind IN ('model', 'prompt', 'tool', 'workflow')),",
"    name TEXT NOT NULL,",
"    description TEXT NOT NULL DEFAULT '',",
"    current_version INTEGER NOT NULL DEFAULT 1,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    UNIQUE(organization_id, project_id, kind, name)",
");",
"CREATE TABLE IF NOT EXISTS registry_artifact_versions (",
"    id TEXT PRIMARY KEY,",
"    artifact_id TEXT NOT NULL,",
"    version INTEGER NOT NULL,",
"    manifest_json TEXT NOT NULL,",
"    digest TEXT NOT NULL,",
"    labels_json TEXT NOT NULL DEFAULT '[]',",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY(artifact_id) REFERENCES registry_artifacts(id) ON DELETE CASCADE,",
"    UNIQUE(artifact_id, version)",
");",
"CREATE TABLE IF NOT EXISTS marketplace_listings (",
"    id TEXT PRIMARY KEY,",
"    artifact_id TEXT NOT NULL UNIQUE,",
"    publisher_organization_id TEXT NOT NULL,",
"    publisher_project_id TEXT NOT NULL,",
"    slug TEXT NOT NULL UNIQUE,",
"    status TEXT NOT NULL DEFAULT 'published' CHECK (status IN ('published', 'withdrawn')),",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    FOREIGN KEY(artifact_id) REFERENCES registry_artifacts(id) ON DELETE CASCADE",
");",
"CREATE TABLE IF NOT EXISTS marketplace_installs (",
"    id TEXT PRIMARY KEY,",
"    listing_id TEXT NOT NULL,",
"    organization_id TEXT NOT NULL,",
"    project_id TEXT NOT NULL,",
"    artifact_id TEXT NOT NULL,",
"    installed_version INTEGER NOT NULL,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    UNIQUE(listing_id, project_id),",
"    FOREIGN KEY(listing_id) REFERENCES marketplace_listings(id) ON DELETE CASCADE,",
"    FOREIGN KEY(artifact_id) REFERENCES registry_artifacts(id) ON DELETE CASCADE",
");",
"",
"-- 31d. Executable external framework runs, scoped to the calling project",
"CREATE TABLE IF NOT EXISTS framework_executions (",
"    id TEXT PRIMARY KEY,",
"    organization_id TEXT NOT NULL,",
"    project_id TEXT NOT NULL,",
"    framework TEXT NOT NULL,",
"    trace_id TEXT NOT NULL,",
"    status TEXT NOT NULL,",
"    input_json TEXT NOT NULL DEFAULT '{}',",
"    output_json TEXT,",
"    error_json TEXT,",
"    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,",
"    completed_at DATETIME",
");",
"",
"-- 33. Provider playground jobs with retry and timeout policy",
"CREATE TABLE IF NOT EXISTS model_jobs (id TEXT PRIMARY KEY, prompt TEXT NOT NULL, models_json TEXT NOT NULL DEFAULT '[]', priority INTEGER NOT NULL DEFAULT 0, status TEXT NOT NULL DEFAULT 'queued', config_json TEXT NOT NULL DEFAULT '{}', attempts INTEGER NOT NULL DEFAULT 0, max_attempts INTEGER NOT NULL DEFAULT 3, timeout_ms INTEGER NOT NULL DEFAULT 30000, claimed_at DATETIME, next_attempt_at DATETIME, result_json TEXT, error_json TEXT, organization_id TEXT, project_id TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, completed_at DATETIME);",
"CREATE TABLE IF NOT EXISTS model_job_tokens (id INTEGER PRIMARY KEY AUTOINCREMENT, job_id TEXT NOT NULL, model TEXT NOT NULL, token_index INTEGER NOT NULL, token TEXT NOT NULL, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY(job_id) REFERENCES model_jobs(id) ON DELETE CASCADE);",
"CREATE TABLE IF NOT EXISTS webhook_subscriptions (id TEXT PRIMARY KEY, url TEXT NOT NULL, events TEXT NOT NULL DEFAULT '[\"*\"]', secret TEXT NOT NULL, enabled INTEGER NOT NULL DEFAULT 1, organization_id TEXT, project_id TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP);",
"CREATE TABLE IF NOT EXISTS secrets (id TEXT PRIMARY KEY, name TEXT NOT NULL, scope TEXT NOT NULL DEFAULT 'workspace', ciphertext TEXT NOT NULL, iv TEXT NOT NULL, tag TEXT NOT NULL, organization_id TEXT, project_id TEXT, created_at DATETIME DEFAULT CURRENT_TIMESTAMP, rotated_at DATETIME);",
"CREATE TABLE IF NOT EXISTS cryptobiosis_snapshots (id TEXT PRIMARY KEY, workspace_id TEXT NOT NULL, reason TEXT, state_json TEXT NOT NULL, frozen_at DATETIME NOT NULL, thawed_at DATETIME, thawed_by TEXT);",
"CREATE TABLE IF NOT EXISTS agent_runtime_state (",
"    id TEXT PRIMARY KEY,",
"    agent_id TEXT NOT NULL UNIQUE,",
"    workspace_id TEXT,",
"    status TEXT NOT NULL DEFAULT 'intermediate',",
"    current_task TEXT,",
"    reason TEXT,",
"    state_json TEXT NOT NULL DEFAULT '{}',",
"    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP",
");"
];

const { TABLES_TENANCY } = require('./schema-tables-tenancy');
const { CREATE_INDEXES_SQL } = require('./schema-indexes');

const TABLES_EXTENSIONS = [...TABLES_EXTENSIONS_BASE, ...TABLES_TENANCY].join("\n");

module.exports = { TABLES_EXTENSIONS, CREATE_INDEXES_SQL };

