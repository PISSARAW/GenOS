name: genos

services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      NODE_ENV: production
      PORT: 4000
      GENOS_DB_PATH: /data/genos.db
      GENOS_WORKSPACE_ROOT: /workspaces
      GENOS_WORKSPACES_ROOT: /workspaces
      GENOS_ADMIN_TOKEN: ${GENOS_ADMIN_TOKEN:-}
      GENOS_ALLOWED_ORIGINS: ${GENOS_ALLOWED_ORIGINS:-}
      GENOS_CODEX_UNSAFE_BYPASS: ${GENOS_CODEX_UNSAFE_BYPASS:-0}
    init: true
    ports:
      - "127.0.0.1:4000:4000"
    volumes:
      - genos-data:/data
      - type: bind
        source: ${GENOS_WORKSPACES_PATH:-.}
        target: /workspaces
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - "require('http').get('http://127.0.0.1:4000/readyz',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"
      interval: 10s
      timeout: 3s
      start_period: 20s
      retries: 5

  studio:
    build:
      context: ./studio
    depends_on:
      backend:
        condition: service_healthy
    init: true
    ports:
      - "127.0.0.1:3000:80"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://127.0.0.1/"]
      interval: 10s
      timeout: 3s
      start_period: 10s
      retries: 5

volumes:
  genos-data:
