name: incident-42
evidence:
  snapshot_ref: production@incident-42
  incident_at: 2026-08-14T02:17:00Z
  logs:
    - worker stalled before commit
    - cache invalidation completed after request timeout
  metrics:
    - p99 latency spike
    - worker runnable count = 48
  traces:
    - request → cache miss → DB lock wait → timeout
  database_state: sha256:db-state-42
  code_versions: [api@a91e, worker@18bc]
  infrastructure: [prod-eu-west, postgres-16, redis-7]
  preceding_events: [evt-901, evt-902, evt-903]
inferred_crash_signature:
  timing_skew_ms: 73.0
  network_latency_ms: 180.0
  packet_loss_percent: 2.5
  reorder_events: true
  db_isolation: serializable
  concurrency: 48
  cache_eviction_ratio: 0.82
config:
  seed: 42
  initial_universes: 100
  partial_survivors: 11
  descendants_per_survivor: 4
  targeted_descendants: 3
